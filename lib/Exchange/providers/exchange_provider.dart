import '../../base_main_repo.dart';
import '../../transactions_screen_base_provider.dart';

class ExchangeProvider extends TransactionsScreenBaseProvider {
  ExchangeProvider(BaseMainRepository repo, List wallets)
      : super(repo, wallets);
}
